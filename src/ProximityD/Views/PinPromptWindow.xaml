<Window x:Class="ProximityD.Views.PinPromptWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Bluetooth Pairing"
        Height="200" Width="380"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Enter the PIN shown on your phone"
                   FontWeight="Bold" FontSize="14"/>

        <TextBlock Grid.Row="1" Margin="0,5,0,5"
                   Text="{Binding DeviceAddress, RelativeSource={RelativeSource AncestorType=Window}, StringFormat=Device: {0}}"
                   Foreground="Gray"/>

        <TextBox Grid.Row="2" x:Name="PinBox" Padding="6" FontSize="16"
                 MaxLength="16" Margin="0,5,0,0"/>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" Width="80" Margin="0,0,8,0" IsCancel="True" Click="Cancel_Click"/>
            <Button Content="OK" Width="80" IsDefault="True" Click="Ok_Click"/>
        </StackPanel>
    </Grid>
</Window>
